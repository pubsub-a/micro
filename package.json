{
  "name": "@pubsub-a/micro",
  "version": "0.9.6",
  "description": "Reference in-memory PubSub/A implementation",
  "license": "MIT",
  "main": "dist/pubsub.js",
  "typings": "dist/pubsub.d.ts",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "@pubsub-a/interfaces": "^0.7.5",
    "chai": "^4.2.0",
    "mocha": "^8.2.1",
    "typescript": "^4.0.5"
  },
  "scripts": {
    "build": "tsc",
    "test": "mocha tests/spec/*.js -- --max-old-space-size=8192"
  }
}
