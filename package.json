{
  "name": "pubsub-a-micro",
  "private": false,
  "version": "0.1.0",
  "description": "Reference PubSub/A implementation",
  "license": "MIT",
  "main": "dist/pubsub.js",
  "typings": "dist/pubsub.d.ts",
  "files": [
    "dist"
  ],
  "devDependencies": {
    "chai": "^3.5.0",
    "karma": "^1.3.0",
    "karma-chai": "^0.1.0",
    "karma-chrome-launcher": "^2.0.0",
    "karma-cli": "^1.0.1",
    "karma-firefox-launcher": "^1.0.0",
    "karma-mocha": "^1.1.1",
    "karma-phantomjs-launcher": "^1.0.2",
    "mocha": "^3.0.2",
    "pubsub-a-interfaces": "git+https://github.com/pubsub-a/pubsub-a-interfaces.git",
    "typescript": "^2.0.3",
    "uglifyjs": "^2.4.10",
    "webpack": "^1.13.1"
  },
  "scripts": {
    "build": "npm run compile && npm run bundle && npm run uglify",
    "compile": "tsc",
    "bundle": "webpack",
    "node-tests": "mocha tests/spec/*.js",
    "browser-tests": "karma start",
    "test": "npm run node-tests && npm run browser-tests",
    "uglify": "uglifyjs -o dist/bundle/pubsub-a-micro.umd.min.js dist/bundle/pubsub-a-micro.umd.js"
  }
}
